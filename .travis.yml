language: php
php:
    - '7.2'
before-install:
    - composer self-update
install:
    - composer install
    - cp .env.dist .env
    - touch var/petregister.db
    - php bin/console d:d:c
    - php bin/console d:s:u --force
